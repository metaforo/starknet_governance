#/bin/bash
# protostar deploy $1 --network testnet --account-address $PROTOSTAR_ACCOUNT --max-fee auto
protostar deploy $1 --network testnet --account-address $PROTOSTAR_ACCOUNT --max-fee auto --inputs $PROTOSTAR_ACCOUNT